Removed the dependency on an external CSV file.

The Air Passengers dataset (1949–1960) is hardcoded into the code.

The rest of the preprocessing, LSTM model creation, training, prediction, and plotting steps are unchanged.
